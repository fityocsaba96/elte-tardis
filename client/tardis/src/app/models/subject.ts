export class Subject {
    constructor(
        courseName: string,
        courseCode: string,
        time: string,
        courseType: string,
        professor: string,
        conflict: boolean
    ) { }
}